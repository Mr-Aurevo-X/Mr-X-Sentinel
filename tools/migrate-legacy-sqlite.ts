/**
 * Migration one-shot depuis les SQLite legacy (Mr-X-Bot / Mr-X-Shadow).
 * Usage: tsx tools/migrate-legacy-sqlite.ts --bot-db path/to/economy.db --shadow-db path/to/bot.db
 *
 * Phase 4 — adapter les requêtes selon les schémas source.
 */
console.log("Script de migration legacy — configure les chemins SQLite et DATABASE_URL puis implémente les imports.");
